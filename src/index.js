/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';

AppRegistry.registerComponent('fax-app', () => App);
AppRegistry.runApplication('fax-app', {
  rootTag: document.getElementById('root'),
});
